# Demonstration data
`data/clean/demo_clean_market_data.csv` is synthetic and is included only so the code structure can be inspected immediately. Do not submit it as Binance output. Run `part1_build_dataset.py` to generate the required live dataset, then continue the pipeline.
